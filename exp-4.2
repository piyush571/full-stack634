const express = require('express');
const app = express();
const PORT = 3000;

app.use(express.json());

let cards = [
  { "id": 1, "suit": "Hearts", "value": "Ace" },
  { "id": 2, "suit": "Spades", "value": "King" },
  { "id": 3, "suit": "Diamonds", "value": "Queen" }
];
let nextId = 4;

// --- ADD THIS NEW ROOT ROUTE ---
app.get('/', (req, res) => {
  res.status(200).json({ 
    message: 'Welcome! API is running.',
    availableRoutes: ['GET /cards', 'GET /cards/:id', 'POST /cards', 'DELETE /cards/:id'] 
  });
});

// --- EXISTING ROUTES ---
app.get('/cards', (req, res) => {
  res.status(200).json(cards);
});

// ... (the rest of your /cards routes) ...

app.listen(PORT, () => {
  console.log(🃏 Card collection API server is running on http://localhost:${PORT});
});
